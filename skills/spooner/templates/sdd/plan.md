# {feature} — plan

## Slices

One entry per slice: what changes, which files, how it verifies independently.

## Verification

- Per slice: the exact commands/checks that prove the slice works
- Acceptance: how the full feature is verified before shipped

## Rollback

- What to restore if a step breaks (git restore / revert)
