# Node
node_modules/
npm-debug.log*

# Python (skills-ref validation, etc.)
__pycache__/
*.pyc
.venv/

# Build artifacts (this project is zero-build; listed defensively)
dist/
build/
out/

# OS / editor
.DS_Store
*.swp
.idea/

# Internal Chinese design archive (local-only; not suitable for the public repo)
docs/
HANDOFF.md

# check baseline ledger (local record, not shared — the manifest .ai-native.yml is tracked)
.ai-native/

# ZCode local tool data (plans archive, etc.)
.zcode/

# Claude Code local settings (user-level state, not shared)
**/.claude/settings.local.json
